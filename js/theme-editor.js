const ThemeEditor=ctx=>({resetTheme(){ctx.themeColors={...DEFAULTS.themeColors};ctx.siteSettings={...ctx.siteSettings,...DEFAULTS.siteSettings};ctx.loaderSettings={...DEFAULTS.loaderSettings};ctx.annBarSettings={...DEFAULTS.annBarSettings};ctx.persist()},resetFooter(){Object.assign(ctx.siteSettings,DEFAULTS.siteSettings);ctx.persist()},resetPageNames(){ctx.pageNames={...DEFAULTS.pageNames};ctx.customPages.forEach(p=>ctx.pageNames[p.id]=p.title);ctx.persist()}});