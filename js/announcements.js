const Announcements=ctx=>({addAnnouncement(){if(!ctx.newAnnText)return Toast.show('Announcement text required');ctx.announcements.push({id:generateId(),text:ctx.newAnnText});ctx.newAnnText='';ctx.persist()},removeAnnouncement(id){ctx.announcements=ctx.announcements.filter(a=>a.id!==id);ctx.persist()},get annBarStyle(){return{background:`linear-gradient(90deg,${ctx.annBarSettings.startColor||'#0F4C81'},${ctx.annBarSettings.endColor||'#2A7F9E'})`,color:ctx.annBarSettings.textColor||'#fff'}},get annScrollStyle(){return{animationDuration:(ctx.annBarSettings.scrollSpeed||50)+'s'}}});